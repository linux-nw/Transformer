// Intentionally empty: each module declares its own plugins. The `core`
// module (pure Kotlin/JVM, Maven Central only) can then be configured and
// tested without ever touching the Android Gradle Plugin / Google's Maven
// repository, which `app` depends on.
